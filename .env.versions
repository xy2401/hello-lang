# hello-lang Docker 镜像锁登记表
# 首次 collect-docker-outputs 工作流必须将实际使用的 builder/runtime tag+digest 写入本文件，
# 只有完成锁定并生成 inventory/session/assert 后，catalog 条目才允许从 documented 改为 verified。
# 禁止 latest、edge、nightly；当前条目仍为 documented，因此不伪造 digest。
