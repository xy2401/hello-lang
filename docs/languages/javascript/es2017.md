# ES2017 (ES8) 规范大典

<script setup>
import { getOutput, getTimeMs } from '../../.vitepress/theme/data/outputsHelper';
</script>

---

## 1. 异步终极语法 (`async` / `await`)
把异步代码用同步扁平的写法表达，解决 Promise 链式调用的书写开销。

## 2. 对象键值对扩展 (`Object.values` & `Object.entries`)
方便遍历与对象转 Map 结构。

```javascript
async function fetchData() {
  return "Async/Await Data Fetched";
}
fetchData().then(res => console.log("ES2017 Output:", res));
console.log("Object.entries:", Object.entries({ a: 1, b: 2 }));
```

<DockerOutput
  image="node:20-alpine"
  sourceFile="demos/js/es2017_demo.js"
/>
