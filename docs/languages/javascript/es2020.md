# ES2020 (ES11) 规范大典

<script setup>
import { getOutput, getTimeMs } from '../../.vitepress/theme/data/outputsHelper';
</script>

---

## 1. 可选链与空值合并 (`?.` & `??`)
彻底终结 `TypeError: Cannot read properties of undefined`。

```javascript
const user = { profile: { email: "alice@example.com" } };
console.log("Optional Chaining user?.profile?.email:", user?.profile?.email);
console.log("Nullish Coalescing null ?? 'default':", null ?? "default");
console.log("BigInt 9007199254740991n + 1n:", 9007199254740991n + 1n);
```

<DockerOutput
  image="node:20-alpine"
  sourceFile="demos/js/es2020_demo.js"
/>
