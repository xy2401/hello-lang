# ES2021 (ES12) 规范大典

<script setup>
import { getOutput, getTimeMs } from '../../.vitepress/theme/data/outputsHelper';
</script>

---

## 1. 字符串全量替换 (`String.prototype.replaceAll`)
替换所有匹配子串，免去书写复杂全局正则表达式 `/g`。

## 2. 逻辑赋值运算符 (`&&=`, `||=`, `??=`)
结合逻辑运算符与赋值操作。

```javascript
const str = "foo-bar-foo";
console.log("String.prototype.replaceAll:", str.replaceAll("foo", "baz"));
let x = null;
x ??= "initialized";
console.log("Logical Assignment x ??= 'initialized':", x);
```

<DockerOutput
  image="node:20-alpine"
  sourceFile="demos/js/es2021_demo.js"
/>
