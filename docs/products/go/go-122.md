# Go 1.22：循环变量作用域

<script setup>
import { getOutput, getTimeMs } from '../../.vitepress/theme/data/outputsHelper';
</script>

> **参考官方文档**: [Go 1.22 Release Notes](https://go.dev/doc/go1.22)  
> Go 1.22 解决了 Go 语言自问世以来困扰无数并发开发者的著名的 **`for` 循环迭代变量作用域陷阱**。

---

## 🐳 容器运行环境 (Runtime Environment)

在标准 Docker 镜像 `golang:1.22-alpine` 中执行控制台诊断指令 `go version`：

<DockerOutput
  image="golang:1.22-alpine"
  sourceFile="demos/go/go122_env.out"
/>

---

## 1. 🔄 `for` 循环迭代变量作用域彻底修正
在 Go 1.22 以前，`for` 循环的循环变量是在每次迭代中复用的，导致在循环中启动 Goroutine 闭包时极易引发并发 Bug。Go 1.22 开始，每次迭代均创建全新的变量实例。

```go
// 关联源码: demos/go/loop_scope_demo.go
values := []string{"a", "b", "c"}
for _, v := range values {
    go func() {
        fmt.Println(v) // Go 1.22 原生输出 a, b, c，无需手动 v := v！
    }()
}
```

<DockerOutput
  image="golang:1.22-alpine"
  sourceFile="demos/go/loop_scope_demo.go"
/>
