#!/usr/bin/env node
import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const products=['clojure','cpp','csharp','css','go','groovy','html','java','javascript','kotlin','lisp','lua','php','python','ruby','rust','scala','typescript'];for(const id of products){const dir=path.join(root,'demos',id,'docker');fs.mkdirSync(dir,{recursive:true});for(const kind of['inventory','session','assert']){const file=path.join(dir,`${kind}.out.txt`);if(fs.existsSync(file))continue;const body=kind==='inventory'?`PENDING: ${id} 的镜像指纹、PATH 与 vendor bin 完整清单尚未由 Docker 工作流采集。`:kind==='session'?`PENDING: ${id} 的构建/检查与运行会话尚未执行；采集规格见 docs/.vitepress/theme/data/dockerCatalog.ts。`:'RESULT: not executed — documented catalog entry, not verified evidence.';fs.writeFileSync(file,`---\nstatus: documented\ncapturedAt: null\ndockerImage: "unresolved"\nexitCode: -1\n---\n${body}\n`)}}console.log(`Initialized ${products.length} language Docker evidence directories`)
